{
  "version": 2,
  "buildCommand": "cd kuff-next && npm install && npm run build",
  "outputDirectory": "kuff-next/.next",
  "installCommand": "cd kuff-next && npm install",
  "framework": "nextjs",
  "rewrites": [
    {
      "source": "/(.*)",
      "destination": "/kuff-next/$1"
    }
  ]
}
